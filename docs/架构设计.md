# Tinalux 架构设计

> 注意：本文包含一部分历史阶段的设计草案代码块，和当前源码并不完全一致。
> 当前实现请优先参考 [源码核对-当前状态-2026-03-16](./源码核对-当前状态-2026-03-16.md)
> 以及 `include/tinalux/*`、`src/app/UIContext.*`。
>
> 目前最关键的差异有两点：
> 1. 事件分发核心已从历史文档里的 `Application::dispatchEvent()` 下沉到 `UIContext`
> 2. 公开渲染 API 已收口为 `Canvas / RenderContext / RenderSurface / Image`，不再直接对外暴露主要 Skia 类型
>
> 多后端渲染已实现：OpenGL + Vulkan 完整可用，Metal 枚举已就位但实现未开始。

## 架构总览

```
              ┌→ Platform ──→ Core
              │
App (胶水) ───┼→ Rendering ──→ Core + Skia
              │
              └→ UI ─────────→ Core + Skia
```

5 个模块。App 是唯一的胶水层，其他模块**互不依赖**。

| 模块 | 职责 | 依赖 | 不依赖 |
|------|------|------|--------|
| **Core** | 事件类型、日志、数学工具 | 仅标准库 | — |
| **Platform** | 窗口抽象（创建、事件、GL 上下文） | Core + GLFW(桌面)/NDK(Android) | Skia |
| **Rendering** | Skia GPU 上下文、Surface 创建 | Core + Skia | Platform, GLFW |
| **UI** | Widget 树、布局、控件、主题 | Core + Skia | Rendering, Platform |
| **App** | 组装一切、驱动主循环 | Platform + Rendering + UI + Core | — |

**关键约束**：
- Rendering **不 include 任何 Platform 头文件**，通过函数指针参数接收 GL proc address
- UI **不 include 任何 Rendering 头文件**，只面向 SkCanvas 和 core::Event
- 只有 App 知道所有模块的存在

## 渲染管线

```
App::init()
│
├─ rendering::initSkia()
├─ BackendPlan plan(config.backend)          ← Auto / OpenGL / Vulkan / Metal
├─ 遍历 plan.candidates():
│   ├─ windowConfig.graphicsApi = graphicsApiForBackend(backend)
│   ├─ window = platform::createWindow(windowConfig)
│   ├─ contextConfig = { backend, window->glGetProcAddress(),
│   │                    window->vulkanGetInstanceProcAddress(),
│   │                    window->requiredVulkanInstanceExtensions() }
│   ├─ context = rendering::createContext(contextConfig)
│   ├─ surface = rendering::createWindowSurface(context, window)
│   └─ 成功则 break，失败则尝试下一个候选后端

App::run() 每帧循环
│
├─ window->pollEvents()
├─ 如果 needsRedraw:
│   ├─ 检查 framebuffer 尺寸变化，必要时重建 surface
│   ├─ Canvas canvas = surface.canvas()
│   ├─ uiContext.render(canvas, w, h)        ← 布局 + 绘制
│   ├─ rendering::flushFrame(context, surface)
│   └─ window->swapBuffers()
└─ 如果 surface 创建失败:
    └─ tryPromoteNextBackend()               ← 运行时 fallback
```

---

## 一、布局系统（参考 Flutter / Druid）

### 核心模型：Constraints 约束协商

```
父组件
  │ 传下 Constraints{minW, maxW, minH, maxH}
  ▼
子组件::measure(constraints)
  │ 在约束范围内决定自己的尺寸
  │ 返回 SkSize
  ▼
父组件
  │ 收到所有子组件的 SkSize
  │ 运行布局算法（Flex/VBox/HBox）
  ▼
父组件::arrange()
  │ 给每个子组件分配最终 bounds
  ▼
子组件::arrange(bounds)
  │ 递归继续
```

### API

```cpp
namespace tinalux::ui {

struct Constraints {
    float minWidth = 0;
    float maxWidth = std::numeric_limits<float>::infinity();
    float minHeight = 0;
    float maxHeight = std::numeric_limits<float>::infinity();

    // 工厂方法
    static Constraints tight(float w, float h);         // min == max
    static Constraints loose(float maxW, float maxH);   // min = 0
    static Constraints unbounded();                      // 全 infinity

    // 工具
    Constraints withMaxWidth(float w) const;
    Constraints withMaxHeight(float h) const;
    SkSize constrain(SkSize size) const;  // 夹到范围内
    bool isTight() const;
};
```

### 两遍布局流程

```cpp
class Widget {
    // 第一遍：measure（自底向上）
    // 在给定约束范围内，返回自己想要的尺寸
    virtual SkSize measure(const Constraints& constraints) = 0;

    // 第二遍：arrange（自顶向下）
    // 父组件分配最终位置
    virtual void arrange(const SkRect& bounds);
};
```

### 布局管理器

```cpp
class Layout {
public:
    virtual ~Layout() = default;

    // 由 Container 调用：
    // 1. 给每个子组件 measure
    // 2. 根据算法分配位置
    virtual SkSize measure(const Constraints& constraints,
                           std::vector<std::shared_ptr<Widget>>& children) = 0;
    virtual void arrange(const SkRect& bounds,
                         std::vector<std::shared_ptr<Widget>>& children) = 0;
};

class VBoxLayout : public Layout {
public:
    float spacing = 0;
    float padding = 0;

    // measure: 宽取最大子组件宽，高取所有子组件高之和 + spacing
    // arrange: 从上往下逐个放置
    SkSize measure(const Constraints& constraints,
                   std::vector<std::shared_ptr<Widget>>& children) override;
    void arrange(const SkRect& bounds,
                 std::vector<std::shared_ptr<Widget>>& children) override;
};

class HBoxLayout : public Layout {
    // 同理，横向排列
};
```

### Container 如何驱动布局

```cpp
class Container : public Widget {
    SkSize measure(const Constraints& constraints) override {
        if (layout_) {
            return layout_->measure(constraints, children_);
        }
        // 无 layout 时：取第一个子组件的尺寸
        if (!children_.empty()) {
            return children_[0]->measure(constraints);
        }
        return constraints.constrain({0, 0});
    }

    void arrange(const SkRect& bounds) override {
        bounds_ = bounds;
        if (layout_) {
            layout_->arrange(bounds, children_);
        }
    }
};
```

---

## 二、事件系统（参考 Chromium / GTK4）

### 三阶段分发：捕获 → 目标 → 冒泡

```
鼠标点击 (300, 200)
    │
    ├─ 第一步：命中测试（找目标）
    │   root->hitTest(300, 200)
    │   递归找到最前面（Z 序最高）的叶子 Widget
    │   得到：eventPath = [root, panel, button]
    │
    ├─ 第二步：捕获阶段（根 → 目标）
    │   root.onEventCapture(click) → 不拦截
    │   panel.onEventCapture(click) → 不拦截
    │
    ├─ 第三步：目标阶段
    │   button.onEvent(click) → handled = true → 停止
    │
    └─ 第四步：冒泡阶段（目标 → 根，仅当未 handled）
        button → panel.onEvent(click) → root.onEvent(click)
```

### API

```cpp
namespace tinalux::core {

enum class EventType {
    None,
    WindowClose, WindowResize, WindowFocus,
    KeyPress, KeyRelease, KeyRepeat,
    MouseButtonPress, MouseButtonRelease,
    MouseMove, MouseScroll,
    TextInput,
};

class Event {
public:
    virtual ~Event() = default;
    virtual EventType type() const = 0;

    bool handled = false;        // 设为 true 停止后续传播
    bool stopPropagation = false; // 设为 true 阻止冒泡
};

// 具体事件类型
class WindowResizeEvent : public Event { /* width, height */ };
class KeyEvent : public Event { /* key, scancode, mods, type_ */ };
class MouseMoveEvent : public Event { /* x, y */ };
class MouseButtonEvent : public Event { /* button, mods, x, y, type_ */ };
class MouseScrollEvent : public Event { /* xOffset, yOffset */ };
class TextInputEvent : public Event { /* codepoint (uint32_t) */ };

}  // namespace tinalux::core
```

### Widget 事件接口

```cpp
class Widget {
    // 捕获阶段（从根往目标走时调用）
    // 返回 true = 拦截，不再往下传
    virtual bool onEventCapture(core::Event& event) { return false; }

    // 目标 + 冒泡阶段
    // 返回 true = 已消费
    virtual bool onEvent(core::Event& event) { return false; }

    // 命中测试：点 (x,y) 是否落在自己身上
    virtual Widget* hitTest(float x, float y);
};
```

### 事件分发器（在 App 中实现）

```cpp
void Application::dispatchEvent(core::Event& event) {
    if (!rootWidget_) return;

    // 鼠标事件需要命中测试
    if (isMouseEvent(event)) {
        auto [x, y] = getMousePos(event);
        Widget* target = rootWidget_->hitTest(x, y);
        if (!target) return;

        // 构建事件路径：root → ... → target
        std::vector<Widget*> path = buildEventPath(target);

        // 捕获阶段（正序，不含 target）
        for (int i = 0; i < path.size() - 1; i++) {
            if (path[i]->onEventCapture(event)) return;
        }

        // 目标阶段
        target->onEvent(event);
        if (event.handled || event.stopPropagation) return;

        // 冒泡阶段（反序，不含 target）
        for (int i = path.size() - 2; i >= 0; i--) {
            path[i]->onEvent(event);
            if (event.handled || event.stopPropagation) return;
        }
    }

    // 键盘事件发给焦点 Widget
    if (isKeyEvent(event) && focusedWidget_) {
        focusedWidget_->onEvent(event);
    }
}
```

### 焦点管理

```cpp
class Application {
    Widget* focusedWidget_ = nullptr;

    void setFocus(Widget* widget);  // 发 FocusOut → 旧, FocusIn → 新
    void advanceFocus();            // Tab 键切换到下一个 focusable
    void retreatFocus();            // Shift+Tab
};

class Widget {
    virtual bool focusable() const { return false; }
    bool focused() const;
};
```

---

## 三、渲染系统（参考 Flutter / LVGL）

### 脏标记 + 按需重绘

```
Button 文字变了
    │
    ├─ button.markDirty()
    │   └─ dirty_ = true
    │   └─ 向上通知：parent_->onChildDirty()
    │       └─ ... 一直传到 root
    │       └─ App::needsRedraw_ = true
    │
    └─ 下一帧 App::run() 检查 needsRedraw_
        └─ true → 重绘整棵树
        └─ false → 跳过这一帧（省 GPU）
```

### Widget 绘制接口

```cpp
class Widget {
    // 绘制自身。canvas 坐标已变换到 widget 本地空间。
    virtual void onDraw(SkCanvas* canvas) = 0;

    // 框架调用此方法（处理坐标变换和裁剪）
    void draw(SkCanvas* canvas) {
        if (!visible_) return;
        canvas->save();
        canvas->translate(bounds_.x(), bounds_.y());
        canvas->clipRect(SkRect::MakeWH(bounds_.width(), bounds_.height()));
        onDraw(canvas);
        canvas->restore();
    }

    // 标脏
    void markDirty();

protected:
    bool dirty_ = true;
    Widget* parent_ = nullptr;  // 用于向上传播脏标记
};
```

### Container 递归绘制

```cpp
class Container : public Widget {
    void onDraw(SkCanvas* canvas) override {
        // 先画自己的背景（如果有）
        drawBackground(canvas);

        // 再按 Z 序画子组件（先添加的在下层）
        for (auto& child : children_) {
            child->draw(canvas);
        }
    }
};
```

---

## 四、Widget 接口总览（参考 Druid）

5 个核心方法，职责清晰：

```cpp
namespace tinalux::ui {

class Widget {
public:
    virtual ~Widget() = default;

    // ── 布局 ──
    virtual SkSize measure(const Constraints& constraints) = 0;
    virtual void arrange(const SkRect& bounds);

    // ── 事件 ──
    virtual bool onEventCapture(core::Event& event) { return false; }
    virtual bool onEvent(core::Event& event) { return false; }
    virtual Widget* hitTest(float x, float y);

    // ── 绘制 ──
    virtual void onDraw(SkCanvas* canvas) = 0;
    void draw(SkCanvas* canvas);  // 框架调用，处理 save/translate/clip/restore

    // ── 属性 ──
    SkRect bounds() const;
    bool visible() const;
    void setVisible(bool v);
    bool focused() const;
    void setFocused(bool f);
    virtual bool focusable() const { return false; }

    // ── 脏标记 ──
    void markDirty();
    bool isDirty() const;

protected:
    SkRect bounds_ = SkRect::MakeEmpty();
    bool visible_ = true;
    bool focused_ = false;
    bool dirty_ = true;
    Widget* parent_ = nullptr;
};

}  // namespace tinalux::ui
```

---

## 五、具体控件 API

### Container

```cpp
class Container : public Widget {
public:
    void addChild(std::shared_ptr<Widget> child);
    void removeChild(Widget* child);
    void setLayout(std::unique_ptr<Layout> layout);

    SkSize measure(const Constraints& constraints) override;
    void arrange(const SkRect& bounds) override;
    void onDraw(SkCanvas* canvas) override;
    bool onEvent(core::Event& event) override;
    Widget* hitTest(float x, float y) override;

    const std::vector<std::shared_ptr<Widget>>& children() const;

private:
    std::vector<std::shared_ptr<Widget>> children_;
    std::unique_ptr<Layout> layout_;
};
```

### Label

```cpp
class Label : public Widget {
public:
    explicit Label(std::string text);
    void setText(const std::string& text);
    void setFontSize(float size);
    void setColor(SkColor color);

    SkSize measure(const Constraints& constraints) override;
    void onDraw(SkCanvas* canvas) override;
};
```

### Button

```cpp
class Button : public Widget {
public:
    explicit Button(std::string label);
    void setLabel(const std::string& label);
    void onClick(std::function<void()> handler);

    bool focusable() const override { return true; }
    SkSize measure(const Constraints& constraints) override;
    void onDraw(SkCanvas* canvas) override;
    bool onEvent(core::Event& event) override;

private:
    enum class State { Normal, Hovered, Pressed };
    State state_ = State::Normal;
};
```

### Panel

```cpp
class Panel : public Container {
public:
    void setBackgroundColor(SkColor color);
    void setCornerRadius(float radius);
    void onDraw(SkCanvas* canvas) override;
};
```

### TextInput

```cpp
class TextInput : public Widget {
public:
    explicit TextInput(std::string placeholder = "");
    std::string text() const;
    void setText(const std::string& text);

    bool focusable() const override { return true; }
    SkSize measure(const Constraints& constraints) override;
    void onDraw(SkCanvas* canvas) override;
    bool onEvent(core::Event& event) override;

private:
    std::string text_;
    size_t cursorPos_ = 0;
    // 将来：选区、IME
};
```

### Theme

```cpp
struct Theme {
    SkColor background, surface, primary, onPrimary;
    SkColor text, textSecondary, border;
    float cornerRadius, fontSize, fontSizeLarge, padding, spacing;

    static Theme dark();
    static Theme light();
};

const Theme& currentTheme();
void setTheme(Theme theme);
```

---

## 六、Platform 模块 API

```cpp
namespace tinalux::platform {

using GLGetProcFn = void* (*)(const char* name);
using VulkanGetInstanceProcFn = void* (*)(void* instance, const char* name);
using EventCallback = std::function<void(core::Event&)>;

enum class GraphicsAPI {
    OpenGL,
    None,       // Vulkan / Metal 窗口不需要 GL context
};

struct WindowConfig {
    int width = 960;
    int height = 640;
    const char* title = "Tinalux";
    bool resizable = true;
    bool vsync = true;
    GraphicsAPI graphicsApi = GraphicsAPI::OpenGL;
};

class Window {
public:
    virtual ~Window() = default;

    virtual bool shouldClose() const = 0;
    virtual void pollEvents() = 0;
    virtual void waitEventsTimeout(double timeoutSeconds) = 0;
    virtual void swapBuffers() = 0;
    virtual void requestClose() = 0;

    virtual int width() const = 0;
    virtual int height() const = 0;
    virtual int framebufferWidth() const = 0;
    virtual int framebufferHeight() const = 0;
    virtual float dpiScale() const = 0;

    virtual void setClipboardText(const std::string& text) = 0;
    virtual std::string clipboardText() const = 0;
    virtual void setTextInputActive(bool active);
    virtual void setTextInputCursorRect(const std::optional<core::Rect>& rect);

    virtual void setEventCallback(EventCallback callback) = 0;
    virtual GLGetProcFn glGetProcAddress() const = 0;

    // Vulkan 平台支持（默认返回不支持，由具体平台实现覆盖）
    virtual bool vulkanSupported() const;
    virtual std::vector<std::string> requiredVulkanInstanceExtensions() const;
    virtual VulkanGetInstanceProcFn vulkanGetInstanceProcAddress() const;
    virtual bool vulkanPresentationSupported(
        void* instance, void* physicalDevice, uint32_t queueFamilyIndex) const;
    virtual void* createVulkanWindowSurface(void* instance) const;
    virtual void destroyVulkanWindowSurface(void* instance, void* surface) const;
};

std::unique_ptr<Window> createWindow(const WindowConfig& config);

}  // namespace tinalux::platform
```

---

## 七、Rendering 模块 API

**不依赖 Platform。GL/Vulkan 函数指针通过 ContextConfig 参数注入。**

```cpp
namespace tinalux::rendering {

using GLGetProcFn = void* (*)(const char* name);
using VulkanGetInstanceProcFn = void* (*)(void* instance, const char* name);

enum class Backend { Auto, OpenGL, Vulkan, Metal };
enum class PaintStyle { Fill, Stroke };

struct ContextConfig {
    Backend backend = Backend::Auto;
    GLGetProcFn glGetProc = nullptr;
    VulkanGetInstanceProcFn vulkanGetInstanceProc = nullptr;
    std::vector<std::string> vulkanInstanceExtensions;
};

// Pimpl 包装类，公共头文件不暴露 Skia 类型
class Canvas { /* save/restore/translate/clipRect/drawRect/drawRoundRect/... */ };
class Image { /* width/height/size, 从文件/内存/RGBA 创建 */ };
class RenderContext { /* backend() 查询当前后端 */ };
class RenderSurface { /* canvas() 获取绘图画布, snapshotImage() 截图 */ };

void initSkia();
void shutdownSkia();

const char* backendName(Backend backend);
RenderContext createContext(const ContextConfig& config);
RenderSurface createWindowSurface(RenderContext& ctx, platform::Window& window);
RenderSurface createRasterSurface(int width, int height);
void flushFrame(RenderContext& ctx, RenderSurface& surface);

Image loadImageFromFile(std::string_view path);
Image loadImageFromEncoded(std::span<const uint8_t> encodedBytes);
Image createImageFromRGBA(int width, int height,
    std::span<const uint8_t> rgbaPixels, size_t rowBytes = 0);

// Vulkan 延迟初始化（内部使用）
bool tryCreateSkiaVulkanContext(RenderContext& context);

}  // namespace tinalux::rendering
```

---

## 八、App 模块 API

**唯一的胶水层。连接 Platform、Rendering、UI。**

```cpp
namespace tinalux::app {

struct ApplicationConfig {
    platform::WindowConfig window {};
    rendering::Backend backend = rendering::Backend::Auto;
};

class Application final {
public:
    Application();
    ~Application();

    Application(const Application&) = delete;
    Application& operator=(const Application&) = delete;

    bool init(const ApplicationConfig& config = {});
    int run();
    void shutdown();

    void setRootWidget(std::shared_ptr<ui::Widget> root);
    void setOverlayWidget(std::shared_ptr<ui::Widget> overlay);
    void clearOverlayWidget();
    platform::Window* window() const;
    ui::AnimationSink& animationSink();
    void requestClose();
    rendering::Backend renderBackend() const;

    // 主题
    void setTheme(ui::Theme theme);
    ui::Theme theme() const;

    // 性能监控
    FrameStats frameStats() const;
    void setPerfLogConfig(PerfLogConfig config);
    void setDebugHudConfig(DebugHudConfig config);

private:
    struct Impl;                    // Pimpl: BackendPlan + Window + RenderContext + RenderSurface + UIContext
    bool renderFrame();
    bool tryInitializeBackend(      // 尝试指定后端
        const platform::WindowConfig& windowConfig,
        rendering::Backend backend, std::size_t backendIndex);
    bool tryPromoteNextBackend();   // 运行时 fallback
    void resetRenderState();
    void syncTextInputState();

    std::unique_ptr<Impl> impl_;
};

}  // namespace tinalux::app
```

**使用示例**：

```cpp
int main() {
    tinalux::app::Application app;

    // 使用 Auto 后端（非 Apple: Vulkan → OpenGL，Apple: Metal → Vulkan → OpenGL）
    if (!app.init({
        .window = {.width = 960, .height = 640, .title = "Tinalux"},
        .backend = tinalux::rendering::Backend::Auto,
    })) return 1;

    auto root = std::make_shared<tinalux::ui::Panel>();

    auto vbox = std::make_unique<tinalux::ui::VBoxLayout>();
    vbox->spacing = 12;
    vbox->padding = 24;
    root->setLayout(std::move(vbox));

    auto label = std::make_shared<tinalux::ui::Label>("Hello, Tinalux!");
    label->setFontSize(32);
    root->addChild(label);

    auto btn = std::make_shared<tinalux::ui::Button>("Click Me");
    btn->onClick([] { /* ... */ });
    root->addChild(btn);

    app.setRootWidget(root);
    return app.run();
}
```

---

## 九、实现状态（2026-03-16 更新）

### 已实现 ✅

| 模块 | 文件 | 说明 |
|------|------|------|
| Core | Event.h (13+ 种事件类型), EventDispatcher.h | 完整事件类型体系（含 TextComposition） |
| Platform | Window.h, GLFWWindow.h/.cpp | 完整 GLFW 后端（含 Vulkan 平台接口、IME、剪贴板、DPI） |
| Rendering | rendering.h/.cpp, GLContext.cpp, WindowSurface.cpp | 多后端：OpenGL + Vulkan 完整实现 |
| Rendering | RenderHandles.h | Pimpl 隔离 Skia/Vulkan 内部类型 |
| Rendering | RasterSurface.cpp | 离屏软件渲染 surface |
| App | BackendPlan.h/.cpp | 后端候选排序 + fallback 策略 |
| App | Application.h/.cpp (Pimpl) | init/run/shutdown + 多后端选择 + 运行时 fallback |
| App | UIContext.h/.cpp | 事件分发核心 + 布局 + 渲染 + 焦点管理 |
| UI | Constraints.h/.cpp | tight/loose/unbounded/constrain 全部方法 |
| UI | Widget.h/.cpp | 5 方法接口 + draw() 坐标变换 + 脏标记向上传播 + globalBounds |
| UI | Container.h/.cpp | 子组件管理 + Layout 委托 + hitTest 递归 |
| UI | VBoxLayout, HBoxLayout, FlexLayout, GridLayout, ResponsiveLayout | 完整布局体系 |
| UI | Label, ParagraphLabel, RichTextWidget | 单行/多行/富文本 |
| UI | Panel, Button, TextInput, Checkbox, Radio, Toggle, Slider | 基础控件 |
| UI | ScrollView, ListView, Dialog, Dropdown, ProgressBar, ImageWidget | 高级控件 |
| UI | Theme, ThemeManager, ColorScheme, Typography, Spacing | 主题系统 |
| UI | IconRegistry, ResourceManager, ResourceLoader | 资源管理 |
| UI | Animation | 属性动画 + 缓动曲线 |
| Tests | 33 个 smoke test | 含 backend_plan / render_backend / event / layout / controls 等 |

### 已知 Bug ⚠️

详见 [代码审查报告](./代码审查报告.md)。历史关键问题均已修复。

### 未实现 ❌

| 功能 | 说明 |
|------|------|
| Metal 后端 | 枚举值和 fallback 候选已就位，实现未开始 |
| Android | NativeActivity/GameActivity + EGL 后端 |
| 触摸事件 | 触摸 → 鼠标事件映射 |
