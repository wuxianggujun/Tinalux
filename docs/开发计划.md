# Tinalux 开发计划

> 说明：本文件用于维护“阶段路线图”。如果只想看当前源码的真实状态，请优先阅读
> [源码核对-当前状态-2026-03-16](./源码核对-当前状态-2026-03-16.md)。

## 阶段一：渲染闭环 ✅ 已完成

**目标**：GLFW 窗口 + Skia 绘制跑通

- [x] CMake 多模块构建（星形依赖：App→Platform/Rendering/UI→Core）
- [x] tina_glfw 源码集成（桌面窗口后端）+ spdlog FetchContent
- [x] Skia 配置（cmake/SkiaConfig.cmake，Debug/Release 双目标）
- [x] Core: Event 基类 + 全部事件类型（13 种）+ EventDispatcher
- [x] Platform: Window 抽象接口 + GLFWWindow 完整实现
  - 8 个 GLFW 回调（resize/close/focus/key/mouse/cursor/scroll/char）
  - GLFW→Tinalux 事件转换，DPI 坐标修正
  - GLFW 生命周期引用计数管理
- [x] Rendering: initSkia/shutdownSkia + createContext(ContextConfig) + createWindowSurface + flushFrame
  - GL/Vulkan 函数指针注入，不依赖 Platform/GLFW
- [x] App: Application（Pimpl 模式）init/run/shutdown + 渲染循环
  - 脏标记跳帧（无变化时 sleep 8ms）
- [x] 删除旧 Qt 文件

---

## 阶段二：布局系统 ✅ 已完成

**目标**：Constraints 约束布局跑通

- [x] Constraints 结构体（tight/loose/unbounded/constrain/withMaxWidth/withMaxHeight/isTight）
- [x] Widget 基类：measure(Constraints) + arrange(SkRect) + draw(canvas) + markDirty
  - draw() 内部处理 save/translate/clipRect/onDraw/restore
  - globalBounds() 累积父链偏移
  - containsGlobalPoint() 用于命中测试
- [x] Container：子组件管理（addChild/removeChild）+ Layout 委托
- [x] VBoxLayout（纵向排列，spacing/padding）
- [x] HBoxLayout（横向排列，spacing/padding）
- [x] Label（SkFont 文字度量 + 渲染，fontSize/color 可设置）
- [x] Panel（圆角矩形背景 + 子组件容器）
- [x] 布局 smoke test

---

## 阶段三：事件分发 ✅ 已完成（有待修复的小 bug）

**目标**：三阶段事件分发 + 焦点管理

- [x] UIContext::dispatchEvent() — 三阶段分发（捕获→目标→冒泡）
- [x] Container::hitTest() — 反向遍历子组件递归命中测试
- [x] 构建事件路径 buildEventPath(target)
- [x] 鼠标事件走 hitTest → dispatchAlongPath
- [x] 鼠标捕获（mouseCaptureWidget，按下时锁定目标）
- [x] hover 追踪（hoveredWidget + MouseEnter/MouseLeave 合成）
- [x] 焦点管理（focusedWidget_ / setFocus / advanceFocus）
- [x] Tab / Shift+Tab 焦点循环
- [x] 点击自动聚焦（findFocusableWidget 沿父链查找）
- [x] WindowFocus 失焦时清理状态
- [x] 事件 smoke test（三阶段顺序验证 + Button 焦点循环 + 鼠标点击）

### 待修复 bug（详见 [代码审查报告](./代码审查报告.md)）

- [x] Bubble 阶段 off-by-one，根节点收不到冒泡事件
- [x] 键盘事件不走三阶段派发，直接发给 focusedWidget
- [x] 缺少 CursorEnter 回调，鼠标离开窗口时 hover 状态未清理
- [x] TextInput 失焦后 draggingSelection_ 未重置

---

## 阶段四：交互控件 ✅ 已完成

**目标**：Button/TextInput 可交互 + Theme

- [x] Theme 系统（dark/light 预设 + currentTheme/setTheme 全局函数）
- [x] Button（hover/pressed/focused 三态 + onClick 回调 + 键盘激活 Enter/Space）
- [x] TextInput 基础（光标、UTF-8 文本输入、Backspace/Delete/方向键/Home/End）
- [x] TextInput 文本选择（Shift+方向键、Shift+Home/End、Ctrl+A 全选）
- [x] TextInput 鼠标拖拽选择（draggingSelection_ + Shift+Click 扩展）
- [x] TextInput 选择高亮渲染（半透明 primary 色圆角矩形）
- [x] TextInput 选择替换（输入/Backspace/Delete 时替换选中文本）
- [x] TextInput 密码遮蔽（setObscured）
- [x] DemoScene 完整登录界面（Panel + Label + TextInput + Button 联动）
- [x] TextInput smoke test（键盘选择、鼠标拖拽选择、选择替换、Backspace 删选择）
- [x] 脏标记传播验证 + 主题切换验证

### 待修复 bug

- [x] Container::removeChild 崩溃（解引用 moved-from shared_ptr）

---

## 阶段五：完善框架 ✅ 已完成（持续增强中）

**目标**：具备开发简单应用的能力

- [x] 修复所有 P0/P1 bug（详见 [代码审查报告](./代码审查报告.md)）
- [x] ScrollView / 滚动容器（移动端刚需）
- [x] 属性动画框架（插值 + 缓动曲线）
- [x] 更多控件（Checkbox / Radio / Toggle / Slider）
- [x] 图片控件（ImageWidget + Image 资源抽象 + 文件/内存/RGBA 加载）
- [x] SkParagraph 多行文本排版
- [x] 剪贴板支持（通过 Platform 抽象）
- [x] 脏矩形优化（只重绘变化区域而非整棵树）
- [x] 基础性能优化（SkSurface 缓存、measure 结果缓存、TextMetrics 缓存）
- [x] GLFW 键值常量迁移到 core/KeyCodes.h
- [x] 日志门面、分类、轮转、环境变量配置
- [x] Debug HUD / 帧统计 / 周期性能摘要
- [x] 公开 Geometry / Canvas / RenderSurface / Image 抽象
- [x] 测试离屏渲染入口（createRasterSurface）

---

## 阶段六：架构增强 ✅ 大部分完成

**目标**：补齐顶层能力，继续收紧边界，为跨平台和引擎集成铺路

- [x] Application 拆分为 Application（壳）+ UIContext（核心）
- [x] Canvas 抽象层（公开 API 不再暴露 Skia 关键类型）
- [x] Skia 类型从公开头文件中移除（公开 Geometry / Canvas / Image）
- [x] Overlay / Popup / 顶层交互层
- [x] 最小 Dialog 组件（基于 Overlay）
- [x] 组件级渲染缓存基础（Panel 可选 Repaint Boundary）
- [x] IconRegistry / ResourceManager / ResourceLoader 基础能力
- [x] createRasterSurface() 离屏渲染入口
- [x] FlexLayout / GridLayout / ResponsiveLayout
- [x] RichTextWidget 基础能力
- [ ] 更完整的文本后端抽象
- [ ] 资源缓存策略进一步统一

---

## 阶段七：跨平台 + 引擎集成 ⬅️ 当前主线

**目标**：验证 Platform 抽象，加 Android，预留引擎集成

- [x] 渲染后端选择入口（Auto / OpenGL / Vulkan / Metal 枚举 + `createContext()` 统一入口）
- [x] `BackendPlan` 候选排序 + fallback 策略（非 Apple: Vulkan→OpenGL，Apple: Metal→Vulkan→OpenGL）
- [x] `Application::tryInitializeBackend()` 逐个尝试 + `tryPromoteNextBackend()` 运行时降级
- [x] OpenGL 后端完整实现（Skia Ganesh GL）
- [x] Vulkan 后端完整实现（VkInstance + 物理设备选择 + 逻辑设备 + Swapchain + Semaphore 同步）
- [x] Vulkan Skia Ganesh context 延迟创建（`tryCreateSkiaVulkanContext()`）
- [x] Vulkan swapchain OUT_OF_DATE / SUBOPTIMAL 处理 + surface 重建
- [x] Portability enumeration / subset 扩展支持（兼容 MoltenVK）
- [x] Platform Window Vulkan 接口（`vulkanSupported` / `createVulkanWindowSurface` / `vulkanPresentationSupported` 等）
- [x] GLFWWindow 完整实现所有 Vulkan 平台接口
- [x] `WindowConfig::GraphicsAPI` 枚举（OpenGL / None），Vulkan 窗口使用 `GLFW_NO_API`
- [x] `RenderHandles.h` Pimpl 隔离 Skia/Vulkan 内部类型
- [x] `render_backend_smoke` + `backend_plan_smoke` 测试覆盖
- [x] `RuntimeHooks` 运行时 hook 机制（`CreateWindowFn` / `CreateContextFn` / `CreateWindowSurfaceFn`）
- [x] `ScopedRuntimeHooksOverride` RAII 作用域替换
- [x] `ApplicationTestAccess` 测试专用入口（直接调用 `renderFrame()`）
- [x] `application_backend_fallback_smoke` 端到端 fallback 测试（启动时 + 运行时）
- [x] 全量 smoke test 36/36 通过
- [ ] Metal 后端实现（枚举值和 fallback 候选已就位）
- [ ] AndroidWindow（NativeActivity/GameActivity + EGL）
- [ ] 触摸事件映射到 MouseEvent
- [ ] Android CMake 工具链 + Skia ARM64 交叉编译
- [ ] 同一套 Widget 代码双平台运行
- [ ] 可替换渲染后端（Skia / Engine）
- [ ] Linux/X11 文本输入链路真机验证
