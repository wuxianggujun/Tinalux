# Tinalux 项目概述

## 这是什么

Tinalux 是一个从零构建的跨平台 Skia 自绘 UI 框架。每个界面元素——按钮、文本框、面板——都由 Skia 在 GPU 上逐像素绘制，不使用任何操作系统原生控件。

## 为什么做这个

通过亲手实现一个完整的 UI 框架，学习 GUI 底层原理：

- **约束布局算法**：父子组件如何协商尺寸（参考 Flutter BoxConstraints）
- **事件分发模型**：捕获→目标→冒泡三阶段（参考 Chromium/GTK4）
- **Widget 树**：组合模式、递归绘制、脏标记按需重绘
- **文本排版**：字体度量、光标定位、文本选择
- **GPU 渲染管线**：OpenGL 上下文 → Skia GrDirectContext → 帧提交
- **平台抽象**：同一套 Widget 代码跑桌面和移动端

后续目标是集成到自研游戏引擎中作为 UI 模块使用。

## 设计参考

| 参考框架 | 取了什么 |
|---------|---------|
| Flutter / Druid | BoxConstraints 约束布局模型 |
| Chromium / GTK4 | 捕获→目标→冒泡事件分发 |
| LVGL | Widget 直持状态 + 脏标记重绘 |
| Druid | 5 方法 Widget 接口设计 |

## 技术栈

| 组件 | 版本 | 用途 |
|------|------|------|
| C++ | 20 | 语言标准 |
| Skia | Ganesh GL | 2D GPU 渲染引擎 |
| GLFW | 3.4 | 桌面窗口后端（将来 Android NDK） |
| CMake | 3.31+ | 构建系统 |

## 构建

```bash
scripts/syncSkia.sh   # 同步 Skia 源码
cmake -B build -S .    # GLFW 自动下载
cmake --build build
```

## 产物大小

| 构建模式 | EXE 大小 | 说明 |
|---------|---------|------|
| Release | 2.2 MB | 93% 是 Skia，你的代码 ~68 KB |
| Debug | 19 MB | 含完整调试符号 |

## 当前状态（2026-03-15）

**已完成（阶段一~六的主体工作）**：
- 构建系统（CMake 多模块 + GLFW FetchContent + Skia Debug/Release）
- Platform 模块（Window 接口 + GLFWWindow 完整实现，8 个事件回调，DPI 支持）
- Rendering 模块（GL 上下文创建、窗口 Surface、离屏 Surface、帧提交，不依赖 Platform）
- UI 模块（Constraints 约束布局、Widget 基类、Container、VBox/HBox 布局、UIContext）
- 控件：Label、ParagraphLabel、Panel、Button、Checkbox、Radio、Toggle、Slider、ScrollView、ListView、TextInput、ImageWidget、Dialog
- Theme 系统（dark/light）
- 三阶段事件派发（capture→target→bubble）+ hitTest + 鼠标捕获 + hover 追踪
- 焦点管理（Tab/Shift+Tab 循环 + 点击聚焦）
- 属性动画、剪贴板、多行文本排版（SkParagraph）
- 图片资源抽象（文件加载、内存解码、RGBA 创建、文件缓存、显式缓存清理）
- 日志门面（分类、轮转、环境变量配置）+ Debug HUD + 帧统计
- App 模块（Application Pimpl 模式、UIContext 拆分、主循环、脏矩形重绘、Overlay 顶层树）
- Overlay / Dialog 顶层交互能力
- 最小组件级渲染缓存（Panel 可选离屏缓存）
- 11 个 smoke test（含 image / text_primitives）

**当前主线（阶段六）**：
- 更完整的组件级渲染缓存
- 图片资源缓存淘汰与图标系统
- 文本后端进一步抽象
- Overlay / Popup 使用样例与更完整组件

## 文档索引

| 文档 | 内容 |
|------|------|
| [架构设计](./架构设计.md) | 模块划分、依赖关系、Constraints 布局、三阶段事件、完整 API |
| [设计规则](./设计规则.md) | 架构约束、Skia 类型隔离规则、编码规范、游戏引擎集成预留 |
| [开发计划](./开发计划.md) | 6 阶段路线图 |
| [代码审查报告](./代码审查报告.md) | Bug 列表、性能问题、修复优先级 |
| [目录结构规划](./目录结构规划.md) | 文件结构和 CMake 配置 |
